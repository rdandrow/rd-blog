<?php

/**
 * Unit Test Example
 *
 * Basic unit test example demonstrating Pest PHP syntax.
 * Unit tests are for testing isolated pieces of code (functions, methods, classes)
 * without dependencies on external systems (databases, APIs, file systems).
 */

test('that true is true', function () {
    expect(true)->toBeTrue();
});